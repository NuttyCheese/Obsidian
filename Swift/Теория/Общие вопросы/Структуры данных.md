#data_structures #algorithm #array #linked_list #stack #queue #tree #graph #hash_table #complexity
Swift предоставляет множество встроенных структур данных для эффективного хранения и манипуляций данными. Вот несколько основных структур данных в [[Swift]]:

1. **Массивы ([[Array]]):**
    
    - Представляют упорядоченный список элементов одного типа.  
Swift предоставляет множество встроенных структур данных для эффективного хранения и манипуляций данными. Вот несколько основных структур данных в [[Swift]]:

2. **Массивы (`Array`):**
    
    - Представляют упорядоченный список элементов одного типа.
```swift
var array = [1, 2, 3, 4, 5]
```
**Словари ([[Dictionary]]):**

- Представляют коллекцию уникальных ключей и связанных с ними значений.**Словари (`Dictionary`):**

- Представляют коллекцию уникальных ключей и связанных с ними значений.
```swift
var dictionary = ["name": "John", "age": 25, "city": "New York"]
```
**Множества ([[Set]]):**

- Представляют неупорядоченный набор уникальных значений.**Множества (`Set`):**

- Представляют неупорядоченный набор уникальных значений.
```swift
var set: Set<String> = ["apple", "orange", "banana"]
```
**Очередь ([[Queue]]):**

- Представляет упорядоченную коллекцию элементов с принципом "первым пришёл — первым ушёл" ([[FIFO]]).**Очередь ([[Queue]]):**

- Представляет упорядоченную коллекцию элементов с принципом "первым пришёл — первым ушёл" (FIFO).
```swift
var queue = Queue<Int>()
queue.enqueue(1)
queue.enqueue(2)
```
**Стек ([[Stack]]):**

- Представляет упорядоченную коллекцию элементов с принципом "последним пришёл — первым ушёл" ([[FILO]]).**Стек (`Stack`):**

- Представляет упорядоченную коллекцию элементов с принципом "последним пришёл — первым ушёл" (LIFO).
```swift
var stack = Stack<String>()
stack.push("A")
stack.push("B")
```
**Связанные списки ([[Linked List]]):**

- Состоят из узлов, каждый из которых содержит значение и ссылку на следующий узел.
```swift
class Node<T> {
    var value: T
    var next: Node?
    init(value: T) {
        self.value = value
    }
}

var linkedList = Node(value: 1)
linkedList.next = Node(value: 2)
class Node<T> {
    var value: T
    var next: Node?
    init(value: T) {
        self.value = value
    }
}

var linkedList = Node(value: 1)
linkedList.next = Node(value: 2)
```
**Деревья ([[Tree]]):**

- Иерархическая структура, состоящая из узлов, где каждый узел имеет некоторое количество потомков.**Деревья (`Tree`):**

- Иерархическая структура, состоящая из узлов, где каждый узел имеет некоторое количество потомков.
```swift
class TreeNode<T> {
    var value: T
    var children: [TreeNode] = []
    init(value: T) {
        self.value = value
    }
}

var tree = TreeNode(value: 1)
tree.children = [TreeNode(value: 2), TreeNode(value: 3)]
class TreeNode<T> {
    var value: T
    var children: [TreeNode] = []
    init(value: T) {
        self.value = value
    }
}

var tree = TreeNode(value: 1)
tree.children = [TreeNode(value: 2), TreeNode(value: 3)]
```
Это лишь несколько примеров структур данных в [[Swift]]. Выбор подходящей структуры данных зависит от конкретной задачи и требований к эффективности операций добавления, удаления, поиска и обновления данных.Это лишь несколько примеров структур данных в Swift. Выбор подходящей структуры данных зависит от конкретной задачи и требований к эффективности операций добавления, удаления, поиска и обновления данных.